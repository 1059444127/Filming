<UserControl x:Class="UIH.Mcsf.Filming.Card.View.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:View="clr-namespace:UIH.Mcsf.Filming.Card.View"
             xmlns:vm="clr-namespace:UIH.Mcsf.Filming.Card.ViewModel" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <!--<UserControl.DataContext>
        <vm:ViewModel />
    </UserControl.DataContext>-->
    <StackPanel>
        <Label>Card</Label>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="5*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <View:ControlPanel Grid.Column="0" />
            <View:Preview x:Name="preview" Grid.Column="1" />
            <!--<Button Grid.Row="1" Content="Load Image" Command="{Binding LoadImageCommand}"/>-->
        </Grid>
    </StackPanel>
</UserControl>
